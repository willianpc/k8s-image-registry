apiVersion: v1
kind: Service
metadata:
  labels:
    app: image-registry
  name: image-registry
  namespace: default
spec:
  ports:
  - port: 5000
    protocol: TCP
    targetPort: 5000
  selector:
    app: image-registry
  sessionAffinity: None
  type: LoadBalancer
